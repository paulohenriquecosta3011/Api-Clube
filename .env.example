# Server configuration
HOST=0.0.0.0
PORT=3001

# Database configuration
DB_HOST=
DB_USER=
DB_PASSWORD=
DB_NAME=

# JWT secret for authentication
JWT_SECRET=

# Email configuration
EMAIL_USER=
EMAIL_PASS=

# Control email sending (true in production)
SEND_EMAIL=false